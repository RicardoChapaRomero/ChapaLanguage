PROGRAM
	DIM lado, perimetro, cycle, altura AS INT
    DIM area AS FLOAT
    
    LET lado = 0
    LET perimetro = 0
    LET area = 0.001
    LET cycle = 3

	gosub calcularArea

	SUBPROCEDURE calcularArea:

		while (cycle > 0) do 
            input lado
            input altura

            let perimetro = lado * 3
            let area = lado * altura / 2

            print area

            input cycle
        wend

	RETURN
END